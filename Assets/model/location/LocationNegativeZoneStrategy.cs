using Game_package;

namespace Location_package
{
    class LocationNegativeZoneStrategy : ILocationBattleStrategy
    {
        public void activate(Game game, Location locatin)
        {
            // foreach(Zone zone in locatin.zones){
            //     zone.total = -5;
            // }
            
        }
    }
}